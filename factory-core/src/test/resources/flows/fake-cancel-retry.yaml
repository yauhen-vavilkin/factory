id: fake-cancel-retry
name: Fake Cancel-then-Retry Flow
version: 1.0.0
triggers:
  - event_type: manual
retry_policy:
  max_attempts: 2
  backoff_seconds: [0]
agent_chain:
  - step_id: doomed
    type: AGENT
    worker_id: cancelling-worker
    outputs: [never.md]
