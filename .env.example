# Offline startup does not read a model API key. Use scripts/factory init to
# create the real gitignored local configuration with safe permissions.
FACTORY_POSTGRES_PORT=55432
FACTORY_APP_PORT=18080

# Live mode is explicit. Export its key in the shell; do not store it here.
# ANTHROPIC_API_KEY=...
