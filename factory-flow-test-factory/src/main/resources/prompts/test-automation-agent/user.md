Generate {{framework}} test scripts for the automatable cases in this approved
test plan. Target environment base URL: {{base_url}}

Approved test plan (Markdown with YAML frontmatter; the frontmatter `cases`
list is authoritative):

{{test_plan}}
