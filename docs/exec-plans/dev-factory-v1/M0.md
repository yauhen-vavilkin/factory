# M0 — Reproducible Baseline and Offline Startup

State: `ACCEPTED`

## Dependency

None. Do not start M0 without explicit authorization. Before any work, refresh `STATUS.yaml` HEAD/branch and record dirty-tree drift; never reset user changes.

## Objective

- [x] **M0-01** Establish what really builds and starts without paid model calls or production connectors.

## Required changes

- [x] **M0-02** Add a pinned Maven Wrapper, retain the accepted dependency versions, and classify pure, Docker integration, and explicit live diagnostic tests.
- [x] **M0-03** Add explicit scripted offline model wiring, disable external writes, unify launcher/effective configuration, and make invalid live configuration fail before network use.
- [x] **M0-04** Bind PostgreSQL to loopback and make database/artifact storage, readiness, and restart behavior durable.
- [x] **M0-05** Add thin `scripts/factory` support for init, offline doctor, infra lifecycle, app lifecycle, and validation; make README/Quickstart describe one verified path and enforce local data/secret permissions.

## Invariants

- [x] **M0-06** Preserve harness intelligence, outcome/receipt semantics, existing Test Factory business behavior, and framework versions unless a reproducible scoped blocker requires correction. Never make a hidden paid startup/test call.
- [x] Apply **INV-01** through **INV-10**, especially loopback/external-write policy, explicit offline/live separation, evidence classes, and test-count rules.

## Validation

- [x] **M0-07** Run `./scripts/factory validate --unit` and record selected test names/counts and exit code.
- [x] **M0-07** Run `./scripts/factory validate --integration` and record selected test names/counts and exit code.
- [x] **M0-07** Run `./scripts/factory validate --eval-pack`; retain the accepted Maven command and assert expected non-zero scenarios.
- [x] **M0-07** Start offline without a model key; verify readiness, no outbound model call, durable restart, and clear failure for invalid live config.

## Acceptance criteria

- [x] **M0-08** Clean checkout plus documented prerequisites completes the supported path.
- [x] **M0-08** Baseline report lists tests actually executed, with non-zero selected counts.
- [x] **M0-08** No-key startup works only in explicit offline mode; invalid live config is actionable.
- [x] **M0-08** Restart preserves database and artifacts.
- [x] **M0-08** Every baseline failure is fixed only within scope or M0 is `BLOCKED` with evidence.

## Handoff

Write `reports/M0.md` with starting/ending HEAD, branch, drift, changes, commands, exit codes, test counts, evidence, blockers, and next action. Set `STATUS.yaml` to `ACCEPTED` only after all mandatory evidence passes; otherwise use `BLOCKED` or keep `IN_PROGRESS`.
