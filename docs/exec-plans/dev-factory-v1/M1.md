# M1 — Admission, Repository/Revision, and Profile Contracts

State: `ACCEPTED`

## Dependency

M0 must be `ACCEPTED`.

## Objective

- [x] **M1-01** Freeze unambiguous task intent before expensive execution.

## Required changes

- [x] **M1-02** Implement the versioned bounded TaskRequest and explicit legacy adapter while preserving raw task text.
- [x] **M1-03** Implement atomic submission, semantic hash/runKey idempotency, reject receipts, safe parsing, and retryable transient admission errors while preserving DB deduplication.
- [x] **M1-04** Implement deterministic four-repository resolution, one-time branch-to-full-SHA resolution, exact-SHA retries, and structured blocked results for conflict/missing/multi-repo cases.
- [x] **M1-05** Enforce canonical HTTPS origin/ref security and separate `baseRevision` SHA validation from branch validation.
- [x] **M1-06** Implement static profile detection, trusted profile/VerificationPlan lookup, sandbox-only effective-POM work, and explicit unsupported profiles.
- [x] **M1-07** Freeze canonical ExecutionContract/config snapshots and hashes; expose only a lossless compatible view to the harness.
- [x] **M1-08** Keep material ambiguity blocked and preserve both accepted `MGRENTITLE-172` interpretations until approval.

## Invariants

- [x] Apply **INV-01** through **INV-10**. In particular, input/model text cannot authorize commands, images, mounts, network, secrets, or wider budgets; invalid tasks run neither shell nor model; common engine semantics remain unchanged.

## Validation

- [x] **M1-09** Run `./scripts/factory validate --unit` with non-zero counts.
- [x] **M1-09** Cover schema/parser, key-order/filename canonicalization, replay/rerun, SHA/ref separation, `master`, all resolution blockers, unknown Java, unsupported Node, hostile YAML/URL/XML/partial/symlink, transient DB, and ambiguity preservation.

## Acceptance criteria

- [x] **M1-10** Every valid input resolves to exactly one repo/SHA/profile or structured `BLOCKED`, without fallback invention.
- [x] **M1-10** Invalid input invokes no shell/model; same runKey creates no second execution; hashes reproduce.

## Handoff

Write `reports/M1.md` with required milestone evidence and update `STATUS.yaml`. Advance to M2 only after M1 is `ACCEPTED`.
