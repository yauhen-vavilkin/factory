# M1 — Preparation checkpoint and plan adoption

State: `ACCEPTED`

## Local checkpoint and drift

- Branch `main`; starting and checkpoint HEAD `9f46a1d9f52b96558116452e6b15f24ba8e650bb`.
- `origin/main` pointed to the same commit. The reported remote checkpoint is therefore present locally.
- Initial staged changes deleted the five tracked old-plan M0/M1 milestone, status, and report files. They were restored exactly because this plan requires historical evidence to remain.
- Initial unstaged user changes were `.gitignore` and the old plan pointer in `AGENTS.md`. The unrelated `.gitignore` additions remain unchanged. The scoped `AGENTS.md` pointer was replaced as required by this adoption.
- The handoff package was initially untracked. Its 13 manifested source files passed their SHA-256 checks.
- No product source, test, build, or runtime configuration differs between implementation commit `b6ba5415f8e4ba54736454a36523264968a23e90` and the starting HEAD in the M1 implementation areas.

## Capability evidence

- `InboxTaskFileParser` retains the exact submitted text as `rawTaskText`; `FileInboxTrigger`, `ExecutionContractFactory`, and `HarnessTaskContractView` carry it into admitted payload and trusted execution views.
- `TaskResolutionService` selects one catalog repository and persists its exact revision. Branch input is resolved to a full SHA; an explicit SHA stays exact.
- `TrustedProfileCatalog` owns execution-profile and verification-plan lookup. Task text cannot provide commands or stronger execution settings.
- A valid supported task produces a single resolved intent with repository, exact SHA, profile, verification plan, and explicit preparation unknowns. Material ambiguity or unsupported evidence produces a structured `BLOCKED` decision. Until image, dependency, and baseline preparation exists, `executionReady=false` is explicit and `CodingWorker` stops before sandbox/model execution.
- No preparation defect was reproduced. No product code was changed, no catalog/hash was pruned, and no seed, image, or baseline hash was invented. The Pi-specific fresh-resolution mode, trusted profile, network policy, and task-specific verification plan remain M2 work under section 4 of `ARCHITECTURE.md`.

## Reused evidence

The old `reports/M1.md` records the accepted implementation commit and 396 pure tests, 16 focused PostgreSQL admission/router/engine tests, and the earlier focused contract/parser/resolver checks. Since the relevant implementation has no drift, those broader results were reused; the full unit/integration baseline was not rerun.

## Commands executed

| Command | Exit | Result |
|---|---:|---|
| `git status --short --branch`; `git rev-parse HEAD` | 0 | Recorded branch, HEAD, staged/unstaged/untracked drift. |
| `git diff --stat b6ba541..HEAD -- factory-flow-dev-factory factory-sandbox factory-app scripts/factory` | 0 | Empty; no relevant product drift. |
| `git show HEAD:docs/exec-plans/dev-factory-v1/{STATUS.yaml,reports/M0.md,reports/M1.md}` | 0 | Historical local evidence inspected from HEAD. |
| `shasum -a 256 -c docs/.../MANIFEST.sha256` from repository root | 1 | Manifest paths are package-relative; no hash verdict obtained. |
| `shasum -a 256 -c MANIFEST.sha256` from the package directory | 0 | 13/13 files passed. |
| focused Maven command without writable `MAVEN_USER_HOME` | 1 | Stopped before Maven because the sandbox denied `~/.m2/wrapper` writes; zero tests selected. |
| focused Maven command with writable home but without `-am` | 1 | Reactor dependencies were absent in the fresh local repository; zero tests selected. |
| `MAVEN_USER_HOME=/tmp/factory-m1-maven-home ./mvnw -B -pl factory-flow-dev-factory -am -Dtest=InboxTaskFileParserTest,TaskResolutionServiceTest,ExecutionContractFactoryTest -Dsurefire.failIfNoSpecifiedTests=false test` | 0 | 23 tests; 0 failures, errors, or skips. |
| same focused reactor command with `-Dtest=FileInboxTriggerTest,CodingWorkerTest` | 0 | 27 tests; 0 failures, errors, or skips. |
| `git add AGENTS.md docs/exec-plans/dev-factory-v1 docs/exec-plans/dev-factory-v1-replanned-handoff` | 128 | Sandbox denied creation of `.git/index.lock`; no index update occurred. |
| YAML parse, historical-file hash comparison, `git diff HEAD --check`, and trailing-whitespace scan | 0 | Both statuses parse; four historical milestone/report files equal HEAD; no whitespace error. |

## Plan adoption

- `pi-minimal-v2` is the active remaining roadmap and preserves the accepted HYBRID boundary.
- Old M0/M1 status and reports remain in place. The old M2–M5 roadmap is marked `SUPERSEDED` in its own status.
- M2–M4 and all explicit deferrals remain unimplemented. No runtime Pi integration or preparation generalization was added.

## Outcome

M1-C1, M1-C2, and M1-C3 are satisfied, with no product or specification blocker. The earlier Git-index restriction was limited to that sandboxed session and is resolved in the delivery session. The exact adoption paths are staged without `.gitignore`; the old M0/M1 files are preserved. The resulting commit SHA is returned separately because a commit cannot contain its own final hash.

Start M2 only after this checkpoint is committed and with explicit authorization.
