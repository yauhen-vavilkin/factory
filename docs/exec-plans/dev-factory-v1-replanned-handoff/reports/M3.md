# M3 — First real MODSIDECAR-208 execution

State: COMPLETE for the M3 workflow capability. The recorded benchmark outcome is `FAILED`; no useful solution was demonstrated.

Code checkpoint before this implementation batch: `7310d5f61d92dc880c26ecf72cde785ed20a3109`. The M3 implementation and post-implementation review fixes were committed and pushed after the validation recorded below.

## Scope and implementation

The exact task was `MODSIDECAR-208` in `folio-org/folio-module-sidecar` at base `c13e0383d9283ef554c195cf5357bb3c6eeb4e65`.

The M3 path now includes:

- a thin `MODSIDECAR-208` Markdown-to-task mapping with the complete task text kept in metadata and a fixed verification-plan ID;
- a trusted `java21-pi-unit` contract with exact source preparation, gateway-only coding, fresh dependency resolution and no provider key in the sandbox;
- a task-specific SmallRye configuration probe, README scope check, fresh candidate verification and real Surefire evidence;
- a fixed local `factory-zai` coding gateway with run-token/model/attempt/output limits, plus a separate dependency-only gateway with read-only approved Maven routes;
- Pi model compatibility fields for the Z.AI General API, including the `system` role, Z.AI thinking format and tool streaming;
- live doctor, run, show and export commands, with legacy `GLM_API_KEY` mapped only when the new `FACTORY_MODEL_API_KEY` is empty;
- Pi-mode inert Spring AI wiring so the host application does not require or initialize the legacy Anthropic client.

The upstream remains the selected Z.AI General API. The implementation does not silently switch to the separate Anthropic-compatible or Coding Plan endpoint. The provider label is explicit `factory-zai`, not an ambiguous product alias.

## Acceptance evidence

### M3-C1 and M3-C2 — source and baseline

The exact base was fetched into a fresh temporary checkout. The target has no Maven wrapper, so the approved system Maven was used:

```text
mvn -B -ntp clean test
exit=0; tests=553; failures=0; errors=0; skipped=0
```

The baseline task checker used the target-derived SmallRye Config version `3.17.2` and confirmed `BASELINE=MISSING` for `quarkus.thread-pool.max-threads`. This proves the task check is red on the clean base independently of the successful build. See `evidence/M3/baseline.json`.

### M3-C3 through M3-C5 — candidate and independent verification

The verifier implementation is present and will check default `8`, environment override `12`, README scope, fresh `mvn -B -ntp clean test`, fresh Surefire XML and tree identity. The live coding attempt produced no patch, so these candidate checks correctly did not claim success:

```text
candidate.patch: 0 bytes
verification: FAIL / EMPTY_CANDIDATE
```

The no-op policy therefore held. A full live candidate replay was not possible without making another model call.

### M3-C6 and M3-C7 — live operation and evidence

The explicit live doctor completed three separate requests with HTTP 200 and the requested model `glm-5.3-flash`; its usage is retained separately from benchmark usage. Three live coding run keys were then used:

| Run key | Execution | Provider calls | Outcome | Boundary |
|---|---|---:|---|---|
| `low-001` | `b00501b6-fe69-476e-82bf-da1b310ef30e` | 0 | `FAILED` | First gateway dependency resolution did not complete the baseline |
| `low-002` | `4f6b017b-7711-425b-bc05-dee545e421af` | 0 | `FAILED` | The initial 8 MiB gateway artifact limit rejected required 10–23 MiB Maven artifacts; the limit was corrected afterward |
| `low-003` | `de76efa0-3d5c-4d5b-ad56-e81d78728e9f` | 1 | `FAILED` | Z.AI returned HTTP 400, code 1214, `Incorrect role information` |

`low-003` reached the real Pi process only after the baseline passed. Native Pi evidence shows the requested provider/model, `thinking=high`, one assistant error response, `agent_settled`, zero tool calls and zero reported tokens. Factory rejected the resulting empty patch and completed export/verification artifacts. The gateway recorded the request as streaming, tool-enabled and containing `reasoning_effort`; the exact old request roles were not retained, so the role cause is an evidence-backed compatibility inference from Pi's local URL detection and the fixed gateway URL, not a claim that the provider returned a detailed diagnostic.

After `low-003`, the trusted model profile was corrected to preserve the accepted Z.AI compatibility contract while keeping `reasoning_effort` enabled. The free scripted smoke then observed `system/user` message roles, Z.AI `thinking`, `tool_stream=true`, native read/bash activity, denied direct egress, fresh verification and wrong-candidate rejection. This correction was not re-qualified against the paid provider because the three live attempt budget was exhausted.

All live run artifacts remain under the ignored `.factory/exports/<execution-id>/` directories. Redacted, tracked summaries are in `evidence/M3/`; the original low-003 Pi session is retained in its local export. No PR, Jira, GitHub or external repository write was made, and the target checkout was not modified.

## Post-implementation review

On 2026-09-11 an independent fresh GPT-6 Astra review at medium reasoning inspected the M3 implementation read-only. It identified real boundary defects and no additional live call was made. The working tree now fixes the following:

- baseline and candidate Surefire evidence preserves every baseline report identity and test count, rejects malformed or duplicate summaries, and rejects failures, errors or increased skips;
- preparation, frozen export and verification use a dependency-only gateway/network without provider credentials or a model route;
- Pi results are published to the durable recovery bundle before teardown, including empty candidates and unsettled failures; candidate tree calculation stages changes first;
- the gateway accepts only approved Z.AI bases, requires bounded `max_tokens`, bounds captured model evidence and records streaming metadata;
- Pi validates state before prompt submission and reads event state through synchronized snapshots;
- Docker command output and host Git preparation are bounded and timeout-safe, with explicit `NONE` network isolation;
- startup failure and stop-timeout paths revoke both gateways, `run-eval` maps the exact admission receipt, and export refuses unsafe overwrites.

These are implementation fixes only. The recorded three-attempt live result is unchanged: the corrected Z.AI compatibility profile has not been paid/live re-qualified, and the benchmark remains `FAILED`.

## Validation

- `./scripts/factory validate --unit`: exit 0; 421 tests, 0 failures/errors/skips.
- Focused review regression selection in `factory-flow-dev-factory` plus `factory-sandbox`: exit 0; 34 tests, 0 failures/errors/skips.
- `factory-app` package with reactor dependencies and skipped tests: exit 0.
- `./scripts/factory smoke --pi --scripted`: exit 0; 3 scripted provider calls and 0 paid calls, direct egress denied, native tools passed, independent verification passed, wrong candidate rejected.
- Exact MODSIDECAR-208 base baseline: exit 0; 553 tests, 0 failures/errors/skips; task checker reported the expected missing setting.
- Python syntax, launcher Bash syntax, JSON parsing and `git diff --check`: passed.

Detailed initial check results are in `evidence/M3/checks.txt`; post-review checks are in `evidence/M3/post-review-checks.txt`; live summaries are in `evidence/M3/live-attempts.json`, `evidence/M3/gateway-live.jsonl` and `evidence/M3/low-003-session-excerpt.jsonl`.

## Final result and next action

```text
workflowOperational=true
benchmarkOutcome=FAILED
firstLowSolved=false
costStatus=UNPRICED
```

The model/account charge cannot be asserted from the failed response; the native usage was zero and the run remains explicitly unpriced. No automatic fourth live attempt is allowed. The next bounded action is an explicitly authorized new live run after reviewing the corrected Z.AI compatibility profile; it should be recorded as an M4 attempt with a fresh run key.
