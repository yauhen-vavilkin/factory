# M2 — Pi-backed Factory vertical slice

State: ACCEPTED for the scripted Pi vertical slice. Candidate commit: `8c14083` on `dev/M2-pi-vertical-slice` (parent `e2d7afc`). No push, PR, merge, or paid provider call was made.

## Changed paths

Pi worker/runner and wiring; Docker process-session deadlines and bounded resources; local SHA checkout; trusted verification-plan propagation; legacy end-to-end fixture/parser adaptation; scripted Pi smoke gateway and artifacts.

## Gate evidence

| Command | Exit | Evidence |
|---|---:|---|
| `./scripts/factory validate --unit` | 0 | 398 tests, 0 failures/errors/skips; `.factory/logs/validation/unit.log` |
| `./scripts/factory smoke --pi --scripted` | 0 | `.factory/logs/validation/pi-smoke.log`, raw RPC `.factory/logs/validation/pi-rpc.jsonl`, candidate `.factory/data/artifacts/pi-smoke-candidate.patch`, independent result `.factory/data/artifacts/pi-smoke-verification.json` |
| `mvn verify -B` | 0 | `.factory/logs/validation/mvn-verify-final.log`; reactor and all module test suites SUCCESS |
| focused `DevFactoryEndToEndScenarioTest` | 0 | 1 test, 0 failures; PostgreSQL/Testcontainers inbox admission, patch, trusted check, finalization |

Smoke recorded `provider_calls=1`, `paid_calls=0`, Pi `0.85.1`, Node `22.19.0`, no TTY, non-root container, internal fixed-route gateway, and run-scoped token rather than a provider key. The first real Pi operation is the `get_state` response retained in the raw JSONL exchange.

## M2-C1…M2-C7 mapping

- **M2-C1 — PASS (scripted/runtime evidence):** pinned image, no-TTY direct argv, no project autoload flags, non-root execution, dropped capabilities, CPU/RAM/PID limits, internal network and no paid key in smoke log.
- **M2-C2 — PASS for implemented boundary:** incremental JSONL framing, correlated command waits, settled-state requirement, stdin close, bounded exit and deadline kill are implemented; raw `get_state` response is retained. Provider error/retry matrices remain a follow-up test gap.
- **M2-C3 — PASS for bounded capture seam:** concurrent stdout/stderr capture and max output budget are wired; full native compaction/2 MiB scripted matrix is not exercised by the acceptance smoke.
- **M2-C4 — PASS for enforced controls:** internal gateway topology, run token, container limits, timeout kill and whole-container teardown are wired. Detached-child and destination-denial cases remain limitations of this narrow smoke.
- **M2-C5 — PASS for export seam:** Pi coding exports `git diff --binary HEAD` before teardown and persists session/usage/candidate artifacts; crash/recovery replay is not independently exercised in the smoke.
- **M2-C6 — PASS for green mechanical slice:** real PostgreSQL Factory inbox scenario completes with a changed README, trusted verification, final result and exactly-once/replay coverage; Pi wiring is present and smoke exports an independently checked candidate. This is not a benchmark-quality claim.
- **M2-C7 — PASS for recorded gates:** raw RPC, smoke, candidate, verification and Maven logs are retained; 398 unit tests and full `mvn verify` are green. Provider cost is explicitly zero for scripted calls.

## Limitations

The smoke command is the approved scripted runtime probe rather than a paid/live model run. The full Pi worker path is wired but not invoked by the legacy PostgreSQL fixture; live gateway credentials, real FOLIO execution, broader retry/compaction/cancellation matrices, and benchmark outcome belong to M3/M4. No useful-solution or PR-readiness claim is made.

Кратко по-русски: M2 принят на зелёном scripted-кандидате `8c14083`. Pi 0.85.1 запускается в Docker через Factory boundary, smoke показал `provider_calls=1`, кандидат и независимая проверка сохранены; unit и `mvn verify -B` зелёные. Отчёт: `docs/exec-plans/dev-factory-v1-replanned-handoff/reports/M2.md`.
