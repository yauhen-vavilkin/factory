# M2 — Pi-backed Factory vertical slice

State: ACCEPTED scripted runtime checkpoint; implementation commit: `9f15620052879bca9d8756a1d2c5b4cbc6811a32`.

## Changed paths

- `factory-sandbox/Dockerfile`, `factory-sandbox/pi-models.json`: pinned Pi 0.85.1 image with baked `factory-zai` OpenAI-compatible model profile.
- `scripts/factory`: real scripted gateway/Pi fixture, native tool session, candidate export, fresh verification, Maven gate and wrong-candidate rejection.
- `factory-flow-dev-factory/.../PiWorker.java`: base-to-HEAD/untracked export metadata and independent fresh-sandbox Maven verification.
- `factory-flow-dev-factory/src/main/resources/flows/dev-factory-pi.yaml`: candidate identity artifact is carried into verification.

## Fresh validations

| Command | Exit | Evidence |
|---|---:|---|
| `git diff --check` | 0 | commit preflight |
| `./scripts/factory validate --unit` | 0 | 398 tests, 0 failures/errors/skips; `.factory/logs/validation/unit.log` and `unit-tests.txt` |
| `./scripts/factory smoke --pi --scripted` | 0 | `.factory/logs/validation/pi-smoke.log` |
| `mvn verify -B` | 0 | reactor BUILD SUCCESS; `.factory/logs/validation/mvn-verify-final.log` |
| focused reactor test | 0 | 121 Dev Factory tests + 241 Sandbox tests |

## Runtime evidence

The latest smoke recorded three model requests in `.factory/logs/validation/upstream-requests.jsonl`. `.factory/logs/validation/pi-rpc.jsonl` contains `get_state`, prompt acknowledgement, provider/model `factory-zai/glm-5.3-flash`, native `read`, native `bash`, Pi-created commit, `agent_end`, and `agent_settled`. The candidate was produced by Pi at exact HEAD `d176641702a11dc2993ac111e239e021bd0a27c9`, from base `dbe0fd748425e5433080b5ec855ba970fc63abb6`; metadata is `.factory/data/artifacts/pi-smoke-candidate.json` and the full patch is `.factory/data/artifacts/pi-smoke-candidate.patch`.

Independent verification used fresh checkout state and `mvn -B -ntp verify`; log: `.factory/logs/validation/pi-verify-mvn.log`. The result is `.factory/data/artifacts/pi-smoke-verification.json` and records `PASS`, fresh checkout, Maven verification and wrong-candidate rejection. The image starts with direct no-TTY argv, non-root UID, dropped capabilities and no provider secret.

## M2-C1…M2-C7

- **M2-C1 PASS:** pinned image/provider/model, direct no-TTY Pi argv, no autoload flags, non-root and capability restrictions are evidenced by smoke/image configuration.
- **M2-C2 PASS for exercised path:** correlated RPC, prompt, provider request, Pi retry and settled-only completion are present; broader 401/429 matrix remains unrun.
- **M2-C3 PARTIAL:** bounded concurrent capture is implemented and native retry is observed; the full 2 MiB/forced-compaction matrix is not part of this smoke.
- **M2-C4 PARTIAL:** private fixed-route scripted gateway and workload limits are exercised; detached-child and forbidden-destination tests remain unrun.
- **M2-C5 PASS for smoke candidate:** export is base-to-candidate and includes untracked delta handling; candidate identity and fresh verification are recorded.
- **M2-C6 PASS for scripted smoke; PARTIAL for PostgreSQL Pi E2E:** the real Pi/Docker fixture passes independent checks and rejects the wrong candidate. Existing `DevFactoryEndToEndScenarioTest` remains the legacy `dev-factory`/scripted-ChatModel regression and was not converted in this checkpoint.
- **M2-C7 PASS for fresh mechanical gates:** exact artifacts and logs above exist; provider usage is scripted and paid calls are zero. Full lifecycle/security matrix is not claimed.

## Limitations

No paid/live provider call was made. The production Pi worker now has provider configuration and independent verification logic, while the acceptance smoke is the real Pi path. The PostgreSQL application scenario still covers the legacy flow and is an explicit follow-up before claiming a complete Pi-through-PostgreSQL E2E. M3/live execution is not started.

Кратко по-русски: scripted Pi-срез стал реальным: Pi 0.85.1 через `factory-zai` сделал native read/bash и создал кандидат `d176641…`; независимая fresh-проверка и `mvn verify` прошли, неверный кандидат отклонён. Commit: `9f15620052879bca9d8756a1d2c5b4cbc6811a32`. Отчёт: `docs/exec-plans/dev-factory-v1-replanned-handoff/reports/M2.md`.
