# M2 — Pi-backed Factory vertical slice

State: `IN_PROGRESS` (partial checkpoint; not M2 accepted)

## Checkpoint

- Branch: `dev/M2-pi-vertical-slice`
- Starting HEAD: `ef26d0bf89ebf9153f58308e4296d3ce368752ba`
- First real Pi operation: Docker `get_state` RPC response at `.factory/logs/validation/pi-rpc.jsonl`; raw checked-in exchange: `evidence/M2/r1-pi-rpc.jsonl`.
- Docker image: `factory-pi:jdk21`, base `maven:3.9-eclipse-temurin-21`, Node `22.19.0`, Pi package `@earendil-works/pi-coding-agent@0.85.1`.

## Implemented evidence

- R1 partial: real no-TTY Pi RPC through the production Docker process-session class; native model-selected edit/shell operation is not yet demonstrated.
- R2 partial: argv-only session request, concurrent stdout/stderr capture, bounded per-stream bytes, UTF-8 chunk handling, await-exit and whole-container stop seams.
- R4 partial: separate `dev-factory-pi` descriptor and `file.inbox.pi` event selection; legacy descriptor/worker remains present.
- R5 partial: additive `java21-pi-unit` profile, `GATEWAY_ONLY` execution policy, ARM64 image platform, fresh-resolution freeze allowance for absent dependency seed, and task-specific verification-plan ID.
- R6 partial: scripted smoke builds the pinned image and performs real Pi RPC, but it does not yet execute the Maven fixture through PostgreSQL/Factory or independently verify a candidate.

## Requirement map

| ID | Result | Evidence / limitation |
|---|---|---|
| M2-C1 | PARTIAL | Real image/Pi/no-TTY/no-project-context flags and non-root image; network isolation is `--network none` probe only, not the required gateway topology. |
| M2-C2 | PARTIAL | Correlated `get_state` response and bounded JSONL reader; retry/401/429/model mismatch/agent_end-retry cases unimplemented. |
| M2-C3 | NOT_RUN | No >=2MiB preservation or native compaction scenario. |
| M2-C4 | NOT_RUN | Gateway budget and detached-descendant cases unimplemented. |
| M2-C5 | NOT_RUN | Existing legacy recovery evidence remains; Pi export path is not wired. |
| M2-C6 | NOT_RUN | No full PostgreSQL Factory fixture run; Pi worker currently returns explicit `INCOMPLETE`, never success. |
| M2-C7 | PARTIAL | Existing legacy tests pass and raw RPC path is retained; complete run references/usage/events are not wired. |

## Commands and results

| Command | Exit | Result |
|---|---:|---|
| `docker build --progress=plain -t factory-pi:jdk21 factory-sandbox` | 0 | Pinned Pi image built on Docker host. |
| `docker run ... pi --mode rpc ...` | 0 | Real no-TTY `get_state` response; raw exchange saved. |
| `./mvnw -B -ntp -pl factory-sandbox -am -Dtest=PiRpcProcessSessionIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` | 0 | 1 integration test. |
| `./mvnw -B -ntp -pl factory-flow-dev-factory -am -Dtest=ExecutionContractFactoryTest ... test` | 0 | 5 focused tests. |
| `./mvnw -B -ntp -pl factory-flow-dev-factory -am -Dtest=InboxPropertiesTest,FileInboxTriggerActivationTest,DevFactoryConfigurationWiringTest ... test` | 0 | 6 focused regression tests. |
| `./scripts/factory smoke --pi --scripted` | 0 | Real pinned image/RPC probe; not full-flow acceptance. Log: `.factory/logs/validation/pi-smoke.log`. |
| `./scripts/factory validate --unit` | 0 | 398 tests, zero failures/errors/skips. Log: `.factory/logs/validation/unit.log`; summary: `.factory/logs/validation/unit-tests.txt`. |
| `./mvnw -B -ntp test` | 1 | Full reactor run exposed the constructor-binding regression; fixed and focused tests rerun. |
| `git diff --check && ./mvnw verify -B` | 1 | Exact gate reached the app module but the pre-existing `DevFactoryEndToEndScenarioTest` fixture is rejected because its task constraints contain `checks[].command`; 2 Awaitility timeouts followed. No security/parser weakening was made. |

## Changed areas

`factory-sandbox` process-session API and Docker/local implementations; pinned Pi image definition; Pi flow descriptor and flow-local workers/runner; additive profile/contract adaptations; configurable inbox event type; Pi smoke launcher; M2 evidence.

## Limitations and next action

Do not claim workflow operational or benchmark success. The exact full gate is currently blocked by the legacy end-to-end fixture/parser mismatch in addition to the missing M2 slice. Implement the fixed-route fake gateway and scripted HTTP upstream, then wire `PiCodingRunner` into prepare/coding/export/verify/finalize with a tiny Maven normalization fixture and independent wrong-candidate rejection. Add the required C1–C7 integration cases before acceptance.

Кратко по-русски: M2 пока не принят. Реальный Pi 0.85.1 запускается в Docker и отвечает по no-TTY RPC; unit-проверка 398 тестов зелёная. Полный Factory vertical slice, gateway и независимая проверка кандидата ещё не реализованы. Отчёт: `docs/exec-plans/dev-factory-v1-replanned-handoff/reports/M2.md`.
