# M2 — Pi-backed Factory vertical slice

State: INCOMPLETE (F2 remains blocked). Implementation commits: `ad198dd`, `e28020e`.

## Changed paths

- `factory-app/src/test/java/org/folio/factory/app/DevFactoryPiEndToEndScenarioTest.java` — real inbox → router → PostgreSQL → engine → `dev-factory-pi` acceptance path with scripted Pi boundary.
- `factory-app/src/main/resources/application.yaml`, `factory-flow-dev-factory/.../DevFactoryConfiguration.java`, `.../PiWorker.java` — Pi-capable production image default plus run-scoped gateway/token environment plumbing.
- `factory-sandbox/.../SandboxProperties.java`, `DockerSandboxService.java` and tests — `factory-pi:jdk21` default contract.
- `scripts/factory` — integration image preparation follows the Pi-capable image.
- `evidence/M2/` — git-tracked acceptance excerpts; ignored `.factory` paths are not used as report evidence.
- `factory-flow-dev-factory/.../PiWorkerFinalizationTest.java` — success/failure finalization contract.
- `evidence/M2/gateway-reachability.txt` — production network binding evidence.

## Fresh validations

| Command | Exit | Result | Tracked evidence |
|---|---:|---|---|
| `git diff --check` | 0 | clean | `evidence/M2/gates.txt` |
| `./scripts/factory validate --unit` | 0 | 402 tests; 0 failures/errors/skips | `evidence/M2/gates.txt` |
| `./scripts/factory smoke --pi --scripted` | 0 | Pi 0.85.1, Node 22.19.0, 3 scripted requests, native read/bash, settled session, independent verification | `evidence/M2/smoke-summary.txt`, `pi-rpc-excerpt.jsonl`, `verification.json` |
| `./mvnw -B verify` | 0 | BUILD SUCCESS; mocked Pi E2E trigger and all reactor suites green | `evidence/M2/mvn-verify-excerpt.log` |

## Candidate and runtime evidence

The fresh scripted Pi session produced candidate HEAD `fd953ebaf59ce440db0427bfbabc26179b4b9d5f` from base `60b0c7a26a96b607b85390f4eaa66ca02ffe0a2b`; patch identity is recorded in `evidence/M2/candidate.json`. Independent verification is `PASS`, fresh-checkout, and rejects the wrong candidate in `evidence/M2/verification.json`.

The production default is `factory-pi:jdk21`, built from `factory-sandbox/Dockerfile`; its exact Pi executable and argv, private gateway route, and run-scoped token variables are recorded in `evidence/M2/image-capability.txt`. No real credential is committed or sent to the coding container.

The Maven acceptance log contains the durable trigger line `file.inbox.pi -> dev-factory-pi -> PostgreSQL`; the test is separate from the legacy scripted `ChatModel` regression, which remains unchanged.

## M2-C1…M2-C7 evidence map

- **M2-C1 PASS** — `evidence/M2/image-capability.txt`, `smoke-summary.txt`, `pi-rpc-excerpt.jsonl`.
- **M2-C2 PASS** — `evidence/M2/pi-rpc-excerpt.jsonl` shows correlated `get_state`/`prompt`, provider/model, native tools, `agent_end` and `agent_settled`.
- **M2-C3 PASS for the accepted scripted slice** — `evidence/M2/pi-rpc-excerpt.jsonl` and `verification.json`; the broader stress/compaction matrix remains outside this retry.
- **M2-C4 PASS for the accepted scripted gateway path** — `evidence/M2/smoke-summary.txt` and `image-capability.txt`; broader detached-child/negative-network matrix remains outside this retry.
- **M2-C5 PASS** — `evidence/M2/candidate.json` and `verification.json`.
- **M2-C6 INCOMPLETE** — the Maven log proves submit → Factory engine → PostgreSQL → `dev-factory-pi`, but the app E2E still mocks `SandboxService` and `PiCodingRunner`.
- **M2-C7 INCOMPLETE** — gates and evidence tracking pass, but F2 prevents acceptance.

## Limitations

No paid/live provider call was made. F1 is proven at the production Docker create path, but not by a live HTTP probe. F2 is not complete: the PostgreSQL E2E still mocks both runtime components. The broader lifecycle/security stress matrix is not claimed. M3/live execution is not started.

Кратко по-русски: F1 и F4 исправлены, Maven evidence tracked, gates зелёные; F2 остаётся незакрытым из-за Mockito sandbox/runner в PostgreSQL E2E. Коммиты: `ad198dd`, `e28020e`. Отчёт: `docs/exec-plans/dev-factory-v1-replanned-handoff/reports/M2.md`.
