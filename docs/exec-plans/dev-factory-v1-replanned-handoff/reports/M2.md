# M2 — Pi-backed Factory vertical slice

State: COMPLETE.
Code candidate HEAD: `e3bbfc7b7840169c3453222da61b9e83c46d40c7`.

The implementation and evidence updates are committed locally in the M2 checkpoint; no push was performed. The unrelated user `.gitignore` change was preserved. The resulting commit SHA is returned separately because it cannot be embedded in its own report.

Changed areas: Docker Pi image/network and sandbox lifecycle, full-duplex Pi RPC transport, stopped-workspace trusted export, Pi candidate/verification workers, the PostgreSQL Pi E2E scenario, and the portable scripted evidence gate. The new exporter regression covers committed, staged, untracked, deleted, binary, mode, symlink, and agent-Git-metadata changes.

Fresh validations:

- `./scripts/factory validate --unit`: exit 0, 411 tests, 0 failures/errors/skips — `evidence/M2/gates.txt`.
- `BUILDX_CONFIG=/private/tmp/factory-buildx ./scripts/factory smoke --pi --scripted`: exit 0; Pi 0.85.1, 3 scripted provider calls, gateway path serving, `DIRECT_EGRESS=DENIED`, independent Maven verification PASS, wrong candidate REJECTED — `evidence/M2/smoke-summary.txt`, `evidence/M2/candidate.json`, `evidence/M2/verification.json`, `evidence/M2/pi-rpc-excerpt.jsonl`.
- `MAVEN_USER_HOME="$PWD/.factory/cache/maven-home" ./mvnw -B -pl factory-app -am test -Dgroups=integration -Dtest=DevFactoryPiEndToEndScenarioTest -Dsurefire.failIfNoSpecifiedTests=false`: exit 0; 2 tests, 0 failures/errors/skips, gateway requests=3, candidate exported, independently verified, result SUCCESS — `evidence/M2/mvn-verify-excerpt.log`.
- Focused sandbox/Pi checks: 12 unit tests, 9 Pi-worker/protocol tests, and 2 Docker integration tests; all exit 0. The stopped-workspace test reports `STOPPED_EXPORT_PASS` with all change classes preserved — `evidence/M2/gates.txt`.
- `git diff --check`: exit 0 — `evidence/M2/gates.txt`.
- `BUILDX_CONFIG=/private/tmp/factory-buildx ./scripts/factory evidence-verify`: exit 0 and PASS; it reran the smoke and unit gates and matched the tracked candidate evidence.

M2-C1 PASS: pinned non-root/no-TTY image, read-only root, bounded resources, and explicit writable mounts — `evidence/M2/image-capability.txt`, `evidence/M2/gateway-reachability.txt`.
M2-C2 PASS: correlated RPC/native-tool exchange, settled completion, and retained session evidence — `evidence/M2/pi-rpc-excerpt.jsonl`.
M2-C3 PASS: bounded Pi exchange and independent verification artifacts — `evidence/M2/pi-rpc-excerpt.jsonl`, `evidence/M2/verification.json`.
M2-C4 PASS: gateway-only network, denied direct egress, queue/abort cancellation, whole-workload stop, and stopped-workspace export — `evidence/M2/gateway-reachability.txt`, `evidence/M2/gates.txt`.
M2-C5 PASS: trusted candidate/base identity and clean fresh verification; wrong candidate rejected — `evidence/M2/candidate.json`, `evidence/M2/verification.json`.
M2-C6 PASS: full Spring/PostgreSQL file-inbox → Pi → export → independent Maven verify → finalize flow — `evidence/M2/mvn-verify-excerpt.log`.
M2-C7 PASS: tracked runtime, RPC, candidate, verification, and gate evidence — `evidence/M2/gates.txt`, `evidence/M2/image-capability.txt`.

F1–F4 are green through the final evidence gate. No paid/live provider call was made. The broad grouped `./scripts/factory validate --integration` sweep remains red in untouched `factory-core` tests (35 selected; 3 failures and 1 error) and stops before the M2 modules; this is the known grouped Spring database-test interference documented by M1, not an M2 acceptance failure. The affected M2 integration classes and the full Pi E2E passed independently. M3 is not started.

Кратко по-русски: M2 завершён. Исправлены lifecycle Docker sandbox, full-duplex Pi RPC и trusted export после остановки coding workload; добавлены проверки сохранения всех типов изменений и macOS-совместимый smoke gate. Полный PostgreSQL/Pi E2E и независимая Maven-проверка зелёные; платный/live provider не использовался. Изменения оставлены в рабочем дереве без commit/push. M3 не начинался.
