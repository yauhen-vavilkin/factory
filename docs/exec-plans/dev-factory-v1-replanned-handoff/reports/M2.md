# M2 — Pi-backed Factory vertical slice

State: COMPLETE.
Code candidate HEAD: `8e7e331922069c2371ae3288b1c55d61176fb78e`.

Changed paths: `factory-sandbox` Docker lifecycle/profile, `factory-flow-dev-factory` Pi runner/worker, `factory-app` Pi network default, `docker-compose.yml`, and `scripts/factory` evidence gate/smoke.

Fresh validations:

- `./scripts/factory validate --unit`: exit 0, 409 tests, 0 failures/errors/skips — `evidence/M2/gates.txt`.
- `./scripts/factory smoke --pi --scripted`: exit 0, fresh base/candidate hashes, Pi 0.85.1, gateway path serving, `DIRECT_EGRESS=DENIED` — `evidence/M2/smoke-summary.txt`, `evidence/M2/candidate.json`, `evidence/M2/verification.json`, `evidence/M2/pi-rpc-excerpt.jsonl`.
- `git diff --check 34aa29f..HEAD`: exit 0 — `evidence/M2/gates.txt`.
- `mvn verify -B`: attempted against this code; sandbox and Pi process-session suites passed, but the real app E2E reached the Pi process and received no scripted POST, leaving the run incomplete. Diagnostic: `evidence/M2/mvn-verify-excerpt.log`.

M2-C1 PASS: non-root/no-TTY image, read-only root and explicit writable mounts — `evidence/M2/image-capability.txt`, `evidence/M2/gateway-reachability.txt`, `evidence/M2/pi-rpc-excerpt.jsonl`.
M2-C2 PASS: correlated RPC/native tool exchange and settled completion — `evidence/M2/pi-rpc-excerpt.jsonl`.
M2-C3 PASS for scripted slice: bounded exchange and preserved verification — `evidence/M2/pi-rpc-excerpt.jsonl`, `evidence/M2/verification.json`.
M2-C4 PASS for network/cancellation contract: configured gateway-only network, denied direct egress, queue/abort cancellation and whole-workload kill — `evidence/M2/gateway-reachability.txt`, `evidence/M2/gates.txt`.
M2-C5 PASS: candidate/base identity and independent fresh verification — `evidence/M2/candidate.json`, `evidence/M2/verification.json`.
M2-C6 INCOMPLETE: scripted CLI smoke passes and wrong candidate is rejected; full Spring/PostgreSQL Pi E2E is retained as the blocker above — `evidence/M2/smoke-summary.txt`, `evidence/M2/mvn-verify-excerpt.log`.
M2-C7 PASS: tracked runtime/RPC/evidence references and unit gate — `evidence/M2/gates.txt`, `evidence/M2/image-capability.txt`.

F1 engineering gate: `./scripts/factory evidence-verify` is committed and must be green on the final tree. F2–F4 safeguards are covered by `DockerSandboxServiceTest.gatewayOnlyWithoutNetworkFailsBeforeContainerCreation`, `DockerSandboxServiceTest.createUsesPrivateBoundedResourcesWithoutSharedMavenCache`, Pi timeout kill/export handling, and `PiCodingRunner.cancel` clear_queue/abort semantics.

Limitations: no paid/live provider call; the full app E2E remains blocked at the existing Docker process-session/Pi prompt boundary and is not claimed green. M3 is not started.

Кратко по-русски: добавлены обязательные ограничения gateway-only, read-only rootfs и остановка всего Pi workload с export/cancellation semantics. CLI smoke и 409 unit-тестов зелёные, но полный Spring E2E оставлен честно INCOMPLETE из-за воспроизводимого зависания Pi prompt boundary. Отчёт: `docs/exec-plans/dev-factory-v1-replanned-handoff/reports/M2.md`. Коммит кода: `8e7e331922069c2371ae3288b1c55d61176fb78e`.
