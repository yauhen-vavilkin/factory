Make the Pi-backed Factory workflow execute and independently assess the first real MODSIDECAR-208 task from the local evaluation files.

Work in the existing yauhen-vavilkin/factory checkout. The handoff package is `docs/exec-plans/dev-factory-v1-replanned-handoff/`.
Read applicable AGENTS.md files, the package STATUS.yaml, EXECUTION_RULES.md, LOCAL_RUN.md, the M3 section of MILESTONES.md and reports/M2.md. Read the outcome/candidate rules in ARCHITECTURE.md and the configuration/telemetry sections of PI_INTEGRATION.md as needed. Do not import the old implementation roadmap.

Require accepted real-Pi/Docker mechanical evidence. Record local HEAD and working-tree changes; preserve unrelated work. Reuse the existing runner/flow and finish only the missing FOLIO preparation, checker, local configuration and inspection path.

Deliver this operator capability:
configure `.env` → start the Pi mode → run-eval MODSIDECAR-208 with a fresh runKey → watch → inspect/export candidate, independent verification and usage.

Implement a thin deterministic mapping from the complete `evals/tasks/MODSIDECAR-208.md` to the existing task schema, using:
repository: folio-org/folio-module-sidecar
baseRevision: c13e0383d9283ef554c195cf5357bb3c6eeb4e65
execution profile: java21-pi-unit
verification plan: modsidecar-208-v1

The implementation agent is a benchmark curator, not the target task's coding agent. Do not manually insert the target fix and call it a Pi result. Pi receives only the clean task, approved guidance and pre-solution source—not this prompt/package, solutions, future refs, human PR patches or privileged grader data.

Prove the clean baseline before paid coding. Use approved pinned Maven and fresh private dependency resolution through fixed approved gateway routes, not a shared writable cache. Follow the actual repository's wrapper availability rather than blindly invoking ./mvnw. Historical dependency failures are blockers; never replace the base or upgrade target dependencies for green.

Implement the proportional trusted checker in MILESTONES.md: actual SmallRye resolution of default 8 and environment override 12, README variable/default/description/required flag, clean Maven test execution and fresh nonempty Surefire XML with expected suite/count/skip evidence. Prove task-specific red on the base and correct candidate behavior independently. Use clean trusted checker libraries, not candidate-produced classes or coding cache. Relevant extra tests/NEWS changes are allowed; exact human patch equality is not required.

Factory must stop the whole coding workload, freeze/export the actual candidate, and verify those exported bytes in a fresh environment. Bind the report to exact base/candidate/profile/check identities. Harness claims, process exit zero and Maven exit zero alone cannot produce SUCCESS. Preserve partial/error/cancel outcomes and cleanup separately.

Expose real model/config/request/tool/timing/usage data as specified. Keep raw usage and unknown fields; do not sum streaming/native totals twice or report a fabricated zero cost. Reuse local artifacts and existing API/CLI surfaces rather than introducing an accounting/UI platform.

Validation order: focused automated checks → affected regressions → scripted full-flow smoke → qualified live preflight → one real Low attempt → exported-candidate replay. Execute all M3-C1 through M3-C7 checks.

This task authorizes one bounded live preflight and one benchmark attempt using an already configured operator account and the declared budgets/unpriced policy. Reuse a still-valid preflight for the exact configuration. Do not infer credentials, silently substitute a model/provider, or repeat unsuccessful live attempts until one passes. Missing prerequisites remain explicit blockers; complete independent hermetic work where possible.

Fix ordinary Factory defects autonomously and add focused regressions without changing Pi intelligence, target requirements or the rubric. No external PR/Jira writes, host source mutation, unsafe Docker access or core redesign without a demonstrated blocker.

Write reports/M3.md with exact commands/exits/counts, run ID, effective nonsecret profile, actual task outcome, usage/cost coverage, patch/verification hashes/paths and cleanup evidence. Record workflowOperational and firstLowSolved separately. A genuine failed coding experiment may demonstrate operation; it must never be relabeled as a solved benchmark. Update STATUS.yaml accordingly and commit only your changes when safe.

Return a brief Russian summary of what can now be run, the actual live outcome, report/artifact locations, commit SHA and blockers.
